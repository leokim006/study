#include <iostream>
#include <vector>

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);

    int N, T;
    std::cin >> N >> T;
    std::vector<int *> arr(N);
    for (int i = 0; i < N; ++i) {
        int num;
        std::cin >> num;
        std::vector<int> num_arr(num);
        num_arr[0] = num;
        for (int j = 0; j < num; ++j) {
            num_arr
        }
    }

    return 0;
}
